@if(!empty($context['show']))
    <a href="{{ $context['certificate_url'] }}"
       class="btn btn-outline-secondary w-100 mb-2">
        <i class="bi bi-award me-1" aria-hidden="true"></i>
        Zaświadczenie
    </a>
@endif
